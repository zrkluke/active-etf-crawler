[project]
name = "active-etf-crawler"
version = "0.1.0"
description = "Active ETF holdings tracker from MoneyDJ"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
